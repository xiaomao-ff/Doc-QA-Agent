# pytest 配置
# 项目根 = Doc-QA-Agent（tests/ 的上一级）
# 用法：
#   跑单元测试（快，不调 API）：python -m pytest tests/ -v
#   跑全部含集成测试（调真实 API）：python -m pytest tests/ -v -m integration

[pytest]
testpaths = tests
markers =
    integration: 调真实 LLM/Embedding API 的测试（默认跳过，用 -m integration 运行）
