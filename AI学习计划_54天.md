# AI 应用开发 54 天实习突击计划（2026年7月版 · LangChain 1.x / LangGraph 1.x）

> 📅 本文档基于 2026 年 7 月的**最新官方文档**核校更新：
> LangChain v1 / LangGraph v1（2025-10 发布）、CrewAI v1.15、OpenAI GPT-5.6、Claude 5 / Opus 5、
> SiliconFlow（bge-m3 / bge-reranker-v2-m3 / DeepSeek）等。已淘汰的旧 API 均已标注，不再作为教学主体。

agent必看：
# Role: AI应用开发学习的编程导师
你现在是一位耐心、专业的编程导师。你的首要目标是用通俗易懂的语言“教会我”如何写代码，而不是仅仅直接告诉我代码，让我复制粘贴，无特殊需要，尽量别直接给出代码，要一步一步，手把手，喂饭式教学。每一步告诉我这段代码的意思是什么，用了什么工具，用了什么方法。
—— 注意：你现在使用的是 **LangChain 1.x / LangGraph 1.x**（你机器上已装 langchain 1.3.14）。凡是文档里出现 `RetrievalQA`、`AgentExecutor`、`create_react_agent`、`ConversationBufferMemory` 这类"旧 API"，都要提醒我它们**已弃用**，并给出现代替代写法（`create_retrieval_chain` / `create_agent` / LangGraph 状态与 checkpointer）。

---

## 核心定位

> **你学的是"用大模型"，不是"造大模型"。** 2026年企业招实习生的核心需求是：能用现有LLM API搭建可用的AI应用（RAG知识库、智能客服、AI Agent工作流），而不是去训练模型。你的竞争对手（985学生）也是从零开始学这些，赛道是平的。
>
> **2026 年市场的口味变了**：只看 Naive RAG（检索→拼接→问答）已经不够，面试官更看重 **Agentic RAG**（让模型自己决定何时/如何检索）和 **LangGraph 工作流编排**（状态、持久化、人在回路）。所以这个版本的 L2/L3 会大量用 LangGraph 来做编排。

---

## 总览：五个等级 + 54天分配

| 等级 | 名称 | 天数 | 实习面试权重 |
|------|------|------|------------|
| L0 | Python + 工具链预备 | Day 1-6 (6天) | 10% |
| L1 | LLM API 调用 + Prompt 工程 | Day 7-16 (10天) | 25% |
| L2 | RAG + LangGraph 知识库系统 | Day 17-30 (14天) | 30% |
| L3 | AI Agent 开发（LangGraph / create_agent） | Day 31-42 (12天) | 25% |
| L4 | 全栈项目 + 简历冲刺 | Day 43-54 (12天) | 10% |

> 🔄 **当前状态（8/6）**：Day 26 已完成。**正按「⚡ 求职冲刺版计划」执行**：9 天学完核心 + 16 天求职。L2 权重基本到手（Day 22-26 全通），重点转向 Agentic RAG + 上线项目。

**每天建议投入：4-6小时（上午2h + 下午2h + 晚上复盘1h）**

---

## ⚡ 求职冲刺版计划（8/7-8/31）—— 当前执行（最新，优先看这里）

> **背景**：8/15 前完成技术冲刺，9/1 前拿到实习。当前进度 Day 26 已完成（LangGraph 对话式 RAG）。
> **总原则**：砍广度、保命脉（**Agentic RAG + 上线项目 + 求职**）。原 Day 27-54 压缩为 **9 天冲刺 + 16 天求职**。

### 冲刺段 8/7-8/15（每天 6-8 小时）

| 新日程 | 合并的原计划 | 内容 | 当天产出 |
|--------|------------|------|---------|
| 8/7 | 原28+33 核心 | **Agentic RAG 入门**：`create_agent` + `@tool` 检索工具，学"模型自主决定何时检索" | 研究助手 Agent 可运行 |
| 8/8 | 原34+35 核心 | LangGraph 条件分支 + Agent 防死循环/错误处理 | 稳定版 Agent |
| 8/9 | 原41+47+27精简 | **综合项目A 开发①**：企业知识库 Agentic RAG 定架构 + 中文优化 | 项目骨架 |
| 8/10 | 原43+44+45合并 | **综合项目A 开发②**：FastAPI 把 RAG/Agent 打包成 API | API 可调用 |
| 8/11 | 原46+48/49精简 | **综合项目A 开发③**：前端（Streamlit）+ 对接 API | 可演示项目 |
| 8/12 | 原50 精简版 | **部署上线**：Streamlit Community Cloud / HuggingFace Spaces 免费部署 | 公网链接 |
| 8/13 | 原30/42/51 | 简历 STAR 改写 + GitHub README + 项目打磨 | 简历 + 作品集 |
| 8/14 | 原52 | 面试八股冲刺：RAG/Agent/Prompt/FastAPI 高频 + 项目深挖 | 话术卡 |
| 8/15 | 原53+54 | 模拟面试 ≥2 轮 + 整理投递列表 | 投递启动 |

### 求职段 8/16-8/31（16 天）
- 每日节奏：上午投递 10-15 份 → 下午面试/复盘 → 晚上补弱项
- 渠道：实习僧 / Boss直聘 / 牛客 / 公司官网邮箱海投
- 每周复盘 bad case，迭代回答话术

### 砍掉 / 降级清单
- ❌ **跳过**：CrewAI（原38）、LangSmith（原40）
- ⏬ **降级**：多Agent Supervisor（原37）只了解概念、RAGAS 评估（原29）改手动 bad case、Docker+云服务器（原50）改免费平台
- ⏬ **合并**：Function Calling 原理（原31）并入 `create_agent` 教学；Agent 记忆（原39）精简进 LangGraph 状态

### 为什么敢砍（面试权重支撑）
- L2 的 RAG 你已很扎实（Day 22-26 全通）→ 面试 30% 权重基本到手
- 面试真正看：① 能讲清 Agentic RAG ② 有上线项目 ③ 简历像样——冲刺版全覆盖
- CrewAI / LangSmith 属加分项，面试只问"了解即可"，不值得耗时间

> 原 Day 27-54 每日安排仍保留在下方，供求职期按需回头补。冲刺期以本表为准。

---

## ⚠️ 必读：新旧 API 对照表（面试和写代码到处用）

你在 Day 21-24 学的是旧写法（能跑，但官方已弃用，面试会被追问）。往下走**优先用新写法**；旧写法只在了解概念时保留。

| 功能 | ❌ 旧 API（弃用/已删） | ✅ 现代写法（LangChain 1.x） |
|------|------------------------|------------------------------|
| 文档问答链 | `RetrievalQA.from_chain_type(...)` | `langchain_classic.chains.create_retrieval_chain` + `create_stuff_documents_chain`（见 Day 22） |
| 多轮检索问答 | `ConversationalRetrievalChain` | LangGraph 状态 + 检索/回答节点（见 Day 26/35） |
| 多角度查询 | `MultiQueryRetriever` | LangGraph「查询分析」节点，用 `with_structured_output` 生成多个查询（见 Day 23 说明） |
| 重排序 | （原生 API 调用，你 Day 24 已学，很对） | LangChain 封装：`ContextualCompressionRetriever` + `CrossEncoderReranker`（可选对比） |
| 对话记忆 | `ConversationBufferMemory` / `ConversationSummaryMemory` | LangGraph `State`（messages 列表 + `add_messages`）+ 持久化 `checkpointer`（见 Day 26/39） |
| 构建 Agent | `AgentExecutor`（**已删除**）/ `create_react_agent`（弃用） | `from langchain.agents import create_agent`（见 Day 32） |
| LangGraph 状态 | Pydantic `BaseModel` 做状态 | **TypedDict** + `Annotated[list, add_messages]`（`create_agent` 不支持 Pydantic 状态，见 Day 35） |
| Chroma 持久化 | `Chroma.from_documents(..., persist_directory=...)` | 仍是 `Chroma`，但**建议传 `collection_name`**，或用 `client=chromadb.PersistentClient(path=...)`（见 Day 21 说明） |
| 统一模型入口 | 各家 `ChatOpenAI` / `ChatAnthropic` | `from langchain.chat_models import init_chat_model`（可选，见 Day 12） |
| 结构化输出 | 手写 Pydantic + `json.loads` | `model.with_structured_output(Schema)`（你在 L1 Day 9 已经学过的思路升级版） |

> 一句话记忆：**2026 年写 AI 应用 = LangChain 做工具/检索 + LangGraph 做状态工作流 + `create_agent` 做智能体。**

---

## L0：Python + 工具链预备（Day 1-6）

### 目标
能写 Python 脚本调用 HTTP API，会用 Git，能配开发环境。这部分不变，是地基。

### 核心内容
- **Python 基础**：变量/类型、list/dict、函数定义、if/for、文件读写
- **必备库**：`requests`（HTTP调用）、`json`、`python-dotenv`（环境变量）
- **工具链**：VS Code + Python虚拟环境 + Git + GitHub
- **API入门**：用 requests 调用一个公开API（如天气API），拿到JSON解析

### 每天安排

| 天 | 内容 | 练习 | 预计时长 |
|----|------|------|---------|
| 1 | Python环境搭建 + 基础语法(变量/类型/字符串/数字) | 安装Python/VS Code，运行第一个脚本 | 3h |
| 2 | Python核心：list/dict/元组 + 循环 + 条件判断 | 写一个学生成绩管理系统(纯命令行) | 4h |
| 3 | Python函数 + 文件读写 + 异常处理 | 重构成绩系统，用函数组织，读写文件保存数据 | 4h |
| 4 | requests库 + JSON解析 + API调用 | 调用公开API(天气/新闻)，解析返回JSON | 4h |
| 5 | Git基础 + GitHub + python-dotenv + 虚拟环境 | 创建GitHub仓库，配置API Key管理 | 3h |
| 6 | **复盘+补漏** | 用API做一个小工具(如每日英语句子/汇率查询)，上传GitHub | 4h |

### 常见错误
- 试图学完一整本Python书（不需要！用到什么查什么即可）
- 不理解JSON和dict的互转
- API Key硬编码在代码里（必须用.env）
- 不学Git就开干（面试必问Git经验）

### 进入L1的标准
- [ ] 能独立用Python写一个脚本调用API并处理返回结果
- [ ] 会用Git做add/commit/push，GitHub上有repo
- [ ] 会用`.env`管理敏感信息，知道虚拟环境是什么

---

## L1：LLM API 调用 + Prompt 工程（Day 7-16）

### 目标
能调用主流LLM API，写高质量的Prompt，搭建带UI的对话应用。

### 核心内容
- **LLM API 调用**：OpenAI / Claude API（推荐同时掌握，国内可备选通义千问/DeepSeek）
  - 2026 模型名：OpenAI 用 **GPT-5.6**（Sol/Terra/Luna，便宜用 `gpt-5.6-terra`）、Claude 用 **Sonnet 5 / Opus 5**、低成本用 **DeepSeek**（你已在用 DeepSeek-V4）
  - OpenAI 除了老的 Chat Completions，新增了 **Responses API**（重点功能：内置工具调用、可编程工具、多智能体）——可以了解一下，但实习用 Chat Completions 也完全够
- **Prompt Engineering 核心模式**：
  - 角色设定 + 任务描述 + 输出格式
  - Few-shot / Chain-of-Thought
  - 结构化输出（`with_structured_output`，Pydantic 校验）
- **Streamlit 快速搭建 UI**：st.chat_input / st.chat_message / st.button
- **多轮对话实现**：消息历史管理 + 上下文窗口控制
- **Token 计算与成本控制**

### 每天安排

| 天 | 内容 | 练习 | 预计时长 |
|----|------|------|---------|
| 7 | LLM API调用基础：OpenAI SDK安装 + ChatCompletion | 写脚本实现"提问→回答"，打印token用量 | 4h |
| 8 | Prompt核心技巧：角色+任务+格式+示例 | 做3个不同场景Prompt（翻译/总结/分类）对比不同写法效果 | 4h |
| 9 | 结构化输出：JSON mode + `with_structured_output` + Pydantic | 让LLM输出结构化JSON，用Pydantic校验 | 4h |
| 10 | 多轮对话：消息历史管理 + 上下文窗口 | 实现带记忆的简单对话 | 4h |
| 11 | Streamlit入门 + 对话UI搭建 | 用streamlit搭建聊天界面 | 4h |
| 12 | Claude API（Anthropic SDK）调用 + LangChain `init_chat_model` | 对比OpenAI/Claude响应差异，选合适场景 | 4h |
| 13 | 国内模型：DeepSeek/通义千问API（可选备用） | 调通国内模型API | 3h |
| 14 | **综合项目**：智能写作助手（多轮+结构输出+Streamlit UI） | 完整项目（可切换模型） | 5h |
| 15 | 进阶Prompt：Chain-of-Thought + Few-shot高级技巧 | 做复杂推理任务 + 效果对比实验 | 4h |
| 16 | **复盘+优化**：项目打磨 + 写README + 推GitHub | 整理到作品集 | 3h |

### 常见错误
- Prompt写得太模糊（必须具体到"你是一个XX，任务是XX，输出格式是XX"）
- 不控制token导致成本爆炸
- 不处理API异常（重试机制、超时）
- 以为Prompt写一次就能用（必须迭代测试至少3轮）

### 进入L2的标准
- [ ] 能独立写出让LLM稳定输出结构化结果（JSON）的Prompt
- [ ] 完成一个带UI的多轮对话应用（代码在GitHub）
- [ ] 理解温度、max_tokens、system/user/assistant消息结构、结构化输出
- [ ] 能调用至少2种不同LLM API

---

## L2：RAG + LangGraph 知识库系统（Day 17-30）★面试权重最高，重点

### 目标
能基于本地文档构建知识库问答系统，理解**（召回→重排→生成）完整流程**，并会用 LangGraph 把它编排成可控的工作流，能讲清 2-Step RAG / Agentic RAG / Hybrid RAG 的区别。

### 核心内容（2026 现代版）
- **文本分块（Chunking）**：递归字符分割、语义分块
- **Embedding模型**：bge-m3（中文，你已在用）/ text-embedding-3-small
- **向量数据库**：ChromaDB（本地）+ 向量检索（余弦相似度）
- **Reranker「召回-重排-生成」**：先用向量捞 top-k，再用 bge-reranker 精排，只留 top-n（你 Day 24 已学会原生 API 调用，这是最务实的方式）
- **RAG 三种架构**（面试重点，2026 必问）：
  - **2-Step RAG**：检索永远在生成之前，简单可预测（FAQ、文档机器人）——Day 22-24
  - **Agentic RAG**：让 Agent 自主决定何时/如何检索（用 `create_agent` + `@tool` 检索工具）——Day 28-30
  - **Hybrid RAG**：中间加入查询改写、检索校验、答案校验等环节（用 LangGraph）——Day 25-27
  - **Hybrid RAG 实战配方**：向量检索(k=5) + BM25关键词(jieba分词) 双路召回 → `EnsembleRetriever` RRF融合 → bge-reranker 精排 top-2 → LLM（Day 25 已跑通全链路）
- **LangChain 现代写法**：`DocumentLoader` / `RecursiveCharacterTextSplitter` / `Chroma` / `create_retrieval_chain` + `create_stuff_documents_chain` / LCEL（`|` 管道组合）
- **LangGraph 编排 RAG**：用 `StateGraph` 把"检索→生成→查询分析→校验"做成节点和边，结构化输出做查询分析
- **RAG评估**：RAGAS / 人工 bad case 分析

### 每天安排

| 天 | 内容 | 练习 | 预计时长 |
|----|------|------|---------|
| 17 | Embedding概念 + Embedding API调用 | 对文本生成向量，计算余弦相似度 | 4h |
| 18 | 向量数据库ChromaDB：安装 + 增删查 | 用ChromaDB存储向量，做相似度搜索 | 4h |
| 19 | 文本分块策略：RecursiveCharacterTextSplitter | 对比不同chunk_size/chunk_overlap效果 | 4h |
| 20 | 文档加载：PDF/TXT/MD文档读取（PyMuPDF/文本加载器） | 实现多格式文档加载 | 4h |
| 21 | LangChain向量化入库：Chroma + 自定义Embedding | 用 `Chroma.from_documents` + `SiliconFlowEmbedding` 建库（记得给 `collection_name`） | 4h |
| 22 | **modern RAG 链**：`create_retrieval_chain` + `create_stuff_documents_chain` | 建基于文档的问答链（替代旧 `RetrievalQA`） | 4h |
| 23 | 搜索质量优化：top-k / 阈值 / 查询分析（含解释 `MultiQueryRetriever` 已弃用） | 对比不同参数；用结构化输出改造成多查询 | 4h |
| 24 | Reranker使用（原生 API / BGE-Reranker） | 用 bge-reranker 重排检索结果，对比效果（**你正在学这课，思路正确**） | 4h |
| 25 | Hybrid RAG：向量 + BM25关键词 + RRF融合 + Reranker精排 | 实现"向量+关键词+重排"增强检索（jieba分词；BM25Retriever须用 preprocess_func） | 4h |
| 26 | **LangGraph 做对话式 RAG**：状态 + 检索/回答节点 + 多轮记忆 | 把 RAG 改成 LangGraph 工作流，带多轮对话（`StateGraph`+`add_messages`+`InMemorySaver`+`thread_id`） | 4h |
| 27 | 中文优化：中文分块/Embedding/文档 + 查询分析结构化输出 | 针对中文文档调优 RAG | 3h |
| 28 | **Agentic RAG**：`create_agent` + 检索工具(`@tool`) | 让模型自主决定检索时机，做研究助手 | 5h |
| 29 | 评估：RAGAS指标 + 人工bad case分析 | 评估项目效果，优化至少3个bad case | 4h |
| 30 | **复盘+优化**：项目打磨 + README + GitHub | 整理作品 | 3h |

### 常见错误
- "Naive RAG"：直接拼接文档到Prompt，不做检索质量优化、不控制上下文
- 分块策略一刀切（必须根据文档类型调整）
- 忽视Reranker（这是提升检索质量最有效的单点投入）
- 不会讲清 2-Step / Agentic / Hybrid RAG 的区别（2026 面试必问，一定要能用大白话讲出来）
- 还在用已弃用的 `RetrievalQA` / `MultiQueryRetriever` 而不用 `create_retrieval_chain` / LangGraph
- 中文 BM25 必须配 jieba 分词，且 `BM25Retriever.from_texts` 的参数名是 `preprocess_func`（实测 0.4.2，不是 preprocessing_function）
- `langchain-community` 已官方日落（2026-05 移除），其中的 `BM25Retriever` 属"将死接口"；官方已用 `bm25s` 替换 `rank_bm25`
- RRF 融合别自己写权重，用现成 `EnsembleRetriever(retrievers=[...], weights=[...])`（c 默认 60 不用改）
- LangGraph 对话：记忆键名是 `thread_id`（不是 `thread`）；`add_messages` 传的是**消息列表**（不是 `{"messages":[...]}` 字典外壳）；取"最新用户问题"要用 `reversed(state["messages"])` 倒序找 `HumanMessage`
- `ChatOpenAI` 必须 `from langchain_openai import ChatOpenAI`（`langchain.chat_models` 是旧路径，会 ImportError）

### 进入L3的标准
- [ ] 完成一个文档问答系统（支持PDF/TXT上传+检索+问答），代码在GitHub
- [ ] 会用 `create_retrieval_chain` 搭 RAG，也会用 LangGraph 把它做成带状态的工作流
- [ ] 理解向量搜索 + 关键词搜索的区别，知道什么时候该用什么
- [ ] 能解释chunk_size、overlap、top-k、reranker的作用，能讲清三种 RAG 架构
- [ ] 至少处理过bad case并做过优化

---

## L3：AI Agent 开发（LangGraph / create_agent）（Day 31-42）

### 目标
能基于 LangChain `create_agent` 和 LangGraph 构建 AI Agent，让LLM自主调用工具、规划多步骤任务，并掌握持久化、人在回路等生产级能力。

### 核心内容（2026 现代版）
- **Tool Calling / Function Calling 原理**
- **Agent 新标准**：`from langchain.agents import create_agent`（取代已删除的 `AgentExecutor`、被弃用的 `create_react_agent`）
- **自定义工具**：`@tool` 装饰器；搜索 / 计算器 / 数据库 / 文档查询
- **LangGraph 核心**：`StateGraph` / Node / Edge / 条件分支 / `TypedDict` 状态 / `add_messages` reducer
- **LangGraph 生产级能力**：持久化执行（`checkpointer` 断点续跑）、人在回路（`interrupt()` / 条件断点）、`Command` API
- **多Agent协作**：Supervisor 模式 / `create_agent` 作为子图 / CrewAI（可选）
- **Agent记忆**：短期（State messages）+ 长期（BaseStore）+ 总结（不再用旧 `ConversationSummaryMemory`）
- **可观测性**：LangSmith 追踪

### 每天安排

| 天 | 内容 | 练习 | 预计时长 |
|----|------|------|---------|
| 31 | Function Calling原理 + OpenAI Tool Calling | 实现天气查询/计算器工具调用 | 4h |
| 32 | `create_agent` 入门：模型 + 工具 + system prompt | 构建一个能多步推理的Agent（替代旧 `AgentExecutor`） | 4h |
| 33 | 自定义工具开发：`@tool` + 检索/计算/文件工具 | 给Agent加3个自定义工具 | 4h |
| 34 | Agent错误处理：重试机制 + fallback + 超时 + 防止死循环 | 让Agent稳定运行不崩溃 | 4h |
| 35 | **LangGraph 入门**：State(`TypedDict`) + Node + Edge + 条件分支 | 用 StateGraph 构建有状态工作流 | 4h |
| 36 | **LangGraph 人在回路 + 持久化**：`interrupt()` + checkpointer + `Command` | 构建需要人工确认的审批流程，可断点续跑 | 4h |
| 37 | **Supervisor 多Agent协作** | 构建研究助手：管理员Agent协调搜索/分析/总结 | 5h |
| 38 | CrewAI入门：角色/目标/背景 + 团队 + Flows（可选） | 用CrewAI实现内容创作团队（对比 LangGraph 选型） | 4h |
| 39 | **Agent记忆管理**：State 会话记忆 + 总结 + 长期存储(BaseStore) | 让Agent记住历史交互 | 3h |
| 40 | **Agent可观测性**：LangSmith追踪 + 调试 | 用LangSmith调试Agent行为 | 3h |
| 41 | **综合项目**：智能客服Agent（多工具+多轮+人工兜底） | 完整项目 | 5h |
| 42 | **复盘+优化**：项目打磨 + README + GitHub | 整理作品 | 3h |

### 常见错误
- 还在找 `AgentExecutor` / 用被弃用的 `create_react_agent`（统一用 `create_agent`）
- 用 Pydantic `BaseModel` 做 LangGraph 状态（`create_agent` 不支持，要用 **TypedDict**）
- Agent定义的工具太多导致LLM选错工具（先精后多）
- 没有处理Agent死循环（必须加迭代限制 + fallback）
- 不知道什么时候用 `create_agent`（简单多步）vs 手写 `StateGraph`（需要精细控制）vs 简单 Chain
- 选型不当：简单任务过度设计，复杂流程不持久化

### 进入L4的标准
- [ ] 完成一个带多工具调用的 AI Agent 项目（用 `create_agent` 或 LangGraph）
- [ ] 理解ReAct循环原理 + 能讲清模型"思考→调工具→观察→再思考"的循环（面试必问）
- [ ] 用过至少一个Agent编排框架（LangGraph / CrewAI），能讲清两者选型
- [ ] 会用 LangGraph 状态、checkpointer（持久化）、interrupt（人在回路）
- [ ] 能解释 Chain 和 Agent 的区别

---

## L4：全栈项目 + 求职冲刺（Day 43-54）

### 目标
做一个能展示的全栈AI应用（FastAPI + 前端 + AI能力），准备好简历和面试。

### 核心内容
- **FastAPI 后端**：路由/RESTful API/Pydantic模型/异步
- **前后端对接**：FastAPI ↔ 简单前端（或保留Streamlit但优化UI）
- **Docker 部署**：容器化 + 云服务器部署（阿里云/腾讯云）
- **项目打磨**：选择1-2个L2/L3项目升级为全栈+可演示
- **简历优化**：STAR法则写项目经历
- **面试准备**：高频八股 + 项目深挖 + 行为面试

### 每天安排

| 天 | 内容 | 练习 | 预计时长 |
|----|------|------|---------|
| 43 | FastAPI基础：路由/路径参数/查询参数/Pydantic | 搭建REST API | 4h |
| 44 | FastAPI进阶：异步处理 + 中间件 + CORS + 错误处理 | 给RAG系统加API层 | 4h |
| 45 | FastAPI + LangGraph整合：将RAG/Agent包装为API | 后端API可调用知识库和Agent | 4h |
| 46 | 简单前端：React/Vite或Streamlit优化UI | 前端展示对话和知识库检索结果 | 4h |
| 47 | **项目选定**：选择核心项目（推荐：Agentic RAG 知识库问答 或 AI Agent助手） | 确定架构，分模块开发 | 4h |
| 48 | **核心项目开发**：后端AI能力 | 集中怼后端 | 5h |
| 49 | **核心项目开发**：前端 + API对接 | 集中怼前端 | 5h |
| 50 | **核心项目开发**：部署到云端（Docker + 阿里云ECS） | 让项目可公网访问 | 5h |
| 51 | 简历优化：STAR法则改写项目经历 | 产出3个项目的简历描述 | 3h |
| 52 | 面试准备：高频八股（RAG/Agent/Prompt/API） | 自测+整理话术 | 4h |
| 53 | 面试模拟 + 行为面试准备 | 模拟面试至少3轮 | 4h |
| 54 | 投递策略 + 最终复盘 | 整理投递列表，复习项目亮点 | 3h |

### 常见错误
- 项目只做本地跑，没有上线链接（面试官要看）
- 一个项目做所有功能（聚焦1-2个亮点即可）
- 简历只写技术栈不写成果（要用数据说话）
- 准备面试只背八股不深挖项目（面试官会追着项目问）

---

## 综合项目建议（选1-2个深度打磨，2026 版）

### 项目A：企业智能知识库（推荐首选，做成 Agentic RAG）
- PDF/Word上传 → 分块 → 向量化 → 检索 → 问答
- 用 LangGraph 编排：检索→生成→**查询改写/答案校验**（Hybrid/Agentic）
- 加 Reranker 精排 + 多轮对话记忆（State + checkpointer）
- 用 FastAPI 包装 + 简单前端展示
- **面试亮点**：讲得清"为什么从 2-Step RAG 升级到 Agentic RAG"、处理过中文文档、做过 bad case 优化

### 项目B：AI Agent 工作流助手
- `create_agent` / LangGraph 多工具调用（搜索+计算+文档查询）
- LangGraph 持久化 + Human-in-the-Loop（人工审批/兜底）
- 实现一个完整业务场景（如"自动生成竞品分析报告"）
- **面试亮点**：展示 Agent 自主推理过程、处理过死循环、用过 checkpointer 断点续跑

### 项目C：智能客服系统（RAG + Agent 结合）
- 意图识别 → 知识库检索 → 多轮对话 → 人工兜底
- 用 LangGraph 把整条链路做成状态机，中途 interrupt 转人工
- **面试亮点**：完整业务闭环 + 生产级状态管理

---

## 求职建议

### 目标岗位
- **大模型应用开发实习生**（最匹配）
- AI应用开发实习生 / LLM应用开发实习生
- Python后端（AI方向）实习生

### 投递渠道
- Boss直聘（搜"大模型应用开发 实习"）
- 实习僧 / 牛客网（内推机会多） / 公司官网、公众号

### 简历重点
1. **项目经验**（STAR法）：80%篇幅（技术栈里重点写 **LangGraph / Agentic RAG / create_agent / 向量检索 / Reranker**）
2. **技术栈**放技能栏前半段：Python / LangChain / LangGraph / FastAPI / ChromaDB / LLM API / Git / Reranker
3. **不要写**：C++/Java（除非你非常擅长）、微积分/线性代数

### 面试高频问题（2026 重点）
1. 什么是RAG？Naive RAG 有什么问题？你和 2-Step / Agentic / Hybrid RAG 的区别？
2. Agent 和 Chain 有什么区别？ReAct 循环是怎么工作的？`create_agent` 内部怎么跑？
3. LangGraph 的 State、Node、Edge、checkpointer、interrupt() 都是干嘛的？
4. 你项目中遇到的最大挑战是什么？怎么解决的？
5. 如何处理 LLM 幻觉？（检索质量、Reranker、引用来源、边界校验）
6. 分块策略怎么选？Chunk size 怎么确定？
7. 向量检索和关键词搜索各有什么优劣？Reranker 为什么更准？

---

## 学习资源（截至2026年7月，官方最新）

| 资源 | 说明 |
|------|------|
| [LangChain 官方文档](https://docs.langchain.com) | 最新版文档，含 v1 迁移指南、RAG/Agent 教程 |
| [LangGraph 官方文档](https://docs.langchain.com/oss/python/langgraph) | 状态、图、持久化、人在回路 |
| [LangChain v1 迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1) | 旧 API 怎么改成新 API（你的必读） |
| [OpenAI 模型/API](https://platform.openai.com/docs) | GPT-5.6 系列、Responses API |
| [Anthropic Claude 文档](https://platform.claude.com/docs) | Sonnet 5 / Opus 5 |
| [SiliconFlow 文档](https://docs.siliconflow.cn) | bge-m3 / bge-reranker-v2-m3 / DeepSeek，国内直连，便宜 |
| [ChromaDB 文档](https://docs.trychroma.com) | 本地向量数据库 |
| [Streamlit 文档](https://docs.streamlit.io) | 快速搭 UI |
| [FastAPI 中文文档](https://fastapi.tiangolo.com/zh/) | 后端 API |
| [LangSmith](https://smith.langchain.com) | Agent 调试验证、可观测性 |
| [DeepLearning.AI Short Courses](https://www.deeplearning.ai/short-courses/) | LangChain/Agent 专项课 |

### 常用工具
VS Code + Python扩展 · ChromaDB · Streamlit · FastAPI · LangGraph · LangSmith · Docker Desktop

### 模型推荐（2026）
| 场景 | 推荐 |
|------|------|
| 主力LLM | OpenAI GPT-5.6（Sol/Terra/Luna） / Claude 5（Sonnet/Opus） |
| 低成本推理/国内 | DeepSeek-V4 / Qwen（经 SiliconFlow） |
| Embedding | BAAI/bge-m3（中文）/ text-embedding-3-small |
| Reranker | BAAI/bge-reranker-v2-m3（SiliconFlow 免费）/ Qwen3-Reranker |

---

## 最后提醒

1. **每天都要写代码**。只看不写等于没学。
2. **不要追求完美**。先跑通再优化，不要在第一天就纠结最佳实践。
3. **每个项目都要推上GitHub**。这是你实习面试最大的筹码。
4. **别怕和985竞争**。AI应用开发这个赛道，大家都是从零开始，工程能力比学历重要。
5. **8月开始投递**，7月底做出项目就开始投，到9月开学前还有机会。
6. **旧代码能跑就别慌，但新代码用新 API**。你 Day 21-24 的 `RetrievalQA`、`MultiQueryRetriever` 或 `RetrievalQA` 写法还能运行，但它们已废弃、面试会扣分——后续练习请逐步迁移到 `create_retrieval_chain` / `create_agent` / LangGraph。